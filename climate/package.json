{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "Simple weather application using TypeScript and OpenWeatherMap API",
  "scripts": {
    "build": "tsc",
    "start": "live-server"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}